<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mx.auth.common.dao.AuthOrderDao">

    <select id="getAuthOrderManage" resultType="com.mx.auth.common.model.AuthOrder">
        select * from te_auth_order
        where 1=1
        <if test = "auth_order_id != null and auth_order_id != ''">
           and auth_order_id = #{auth_order_id}
        </if>
        <if test = "order_id != null and order_id != ''">
           and order_id = #{order_id}
        </if>
    </select>
    
</mapper>