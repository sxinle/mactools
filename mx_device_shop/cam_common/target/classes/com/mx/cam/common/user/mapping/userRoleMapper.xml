<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.mx.cam.common.user.dao.UserRoleDao">
    
    <select id="checkUserRoleId" resultType="com.mx.cam.common.user.model.Role">
		SELECT DISTINCT B.role_name FROM tr_authorities A, te_roles B
		WHERE A.role_id = B.role_id
		AND A.role_id in 
 		<foreach  item="item" collection="array" open="(" separator=","  close=")">
			#{item}
		</foreach>
	</select>
    
    <insert id="saveUserRole">
    	INSERT INTO tr_authorities(
    		user_id,
    		role_id
    	)
    	VALUES
    	<foreach collection="array" item="item" index="index"  separator=",">  
        ( 
            #{userID},
            #{item}
        )
        </foreach>
    </insert>
    
    <update id="deleteUserRole">
    	DELETE FROM tr_authorities
    	    WHERE user_id = #{user_id}
    </update>
    
    <select id="selectUserRoles" resultType="com.mx.cam.common.user.model.Role">
    	(
			SELECT
				t1.role_id,
				t1.role_name,
				t1.buildin,
				t1.description
			FROM
				te_roles t1,
				tr_authorities t2
			WHERE
				t1.role_id = t2.role_id
			AND t2.user_id = #{userID}
		)
		UNION
			(
				SELECT
					t3.group_id,
					t3.group_name,
					t3.buildin,
					t3.description
				FROM
					te_group t3,
					te_group_user t4
				WHERE
					t3.group_id = t4.group_id
				AND t4.user_id = #{userID}
			)
    </select>
    
   
</mapper>
