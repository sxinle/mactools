<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.mx.cam.common.user.dao.RoleDao">

    <select id="getRoleManage" resultType="com.mx.cam.common.user.model.Role">
        SELECT * FROM te_roles
        WHERE 1=1
        <if test = "buildin != null and buildin != ''">
        	AND buildin = #{buildin}
        </if>
        <if test = "role_name != null and role_name != ''">
        	AND role_name LIKE CONCAT(CONCAT('%', #{role_name}), '%')
        </if>
        
    </select>
    
    <delete id="deleteRoleManage">
		DELETE FROM te_roles
		WHERE role_id IN 
 		<foreach  item="item" collection="array" open="(" separator=","  close=")">
			#{item}
		</foreach>
	</delete>
	
	<select id="checkRoleName" resultType="int">
        SELECT count(*) FROM te_roles WHERE 1=1
        <if test = "role_id != null and role_id != ''">
       		AND role_id != #{role_id} 
        </if>
       	    AND role_name = #{role_name}
    </select>
    
    <insert id="saveRole">
    	INSERT INTO te_roles(
    		role_id,
    		role_name,
    		buildin,
    		description
    	)
    	VALUES(
    		#{role_id},
    		#{role_name},
    		#{buildin},
    		#{description}
    	)
    </insert>
    
    <update id="updateRole">
    	UPDATE te_roles 
    	    SET 
	    		role_name = #{role_name},
	    		description = #{description}
    		WHERE 
    	   		role_id = #{role_id}
    </update>
    
</mapper>
