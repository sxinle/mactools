<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.mx.cam.common.user.dao.RoleMenuDao">

    <delete id="deleteRoleMenuByMenu">
    	DELETE FROM tr_role_menu where menu_id IN (SELECT id FROM te_menus WHERE id = #{id} OR parentid = #{id})
    </delete>
    
    <delete id="deleteRoleMenuByRole">
		DELETE FROM tr_role_menu
		WHERE role_id IN 
 		<foreach  item="item" collection="array" open="(" separator=","  close=")">
			#{item}
		</foreach>
	</delete>
	
	<select id="getMenuByRole" resultType="com.mx.cam.common.user.model.Menu">
		SELECT menu_id AS id FROM tr_role_menu WHERE role_id = #{id}
	</select>
    
    <insert id="saveRoleMenu">
    	INSERT INTO tr_role_menu(
    		role_id,
    		menu_id
    	)
    	VALUES(
    		#{role_id},
    		#{menu_id}
    	)
    </insert>
    
</mapper>
